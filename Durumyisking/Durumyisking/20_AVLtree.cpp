#include "MyMap.h"
#include <map>

int main()
{
	//pair<int, string> pair1(6, "hello");

	//map<int, string> map;

	//// insert를 하면 key(pair's first)오름차순으로 정렬해서 넣는다.
	//map.insert(pair1);
	//map.insert(make_pair(7, "bye"));

	MyMap<int, int> map;

	


	return 0;
}